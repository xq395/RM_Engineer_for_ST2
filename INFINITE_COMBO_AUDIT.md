# RM工程师无限与伪无限审计

> 基于当前卡牌、遗物和 Power 实际代码。这里的“严格无限”指同一回合内可以恢复循环所需的手牌、能量和借用状态，不依赖随机继续执行。

> 更新说明：数学建模现已移除“本回合费用 -1”和借用后的 1 点能量返还。当前效果为抽 3/4 张牌，借用成功后让本次抽到的牌各免费打出一次；免费效果在该牌首次打出后移除。因此本文下方所有依赖数学建模整回合持续降费的旧循环均已关闭，相关段落仅保留为历史审计记录。

## 结论

审计时发现过以下严格无限：

1. `赛制改革 + 两张学校展出 + 任意一台机甲`：无限能量、无限格挡、无限出牌次数。
2. `赛制改革 + 学校展出 + 召回通知+ + 任意一台机甲`：无限能量、无限格挡、无限出牌次数。
3. `赛制改革 + 学校展出 + 现场答辩`：无限伤害、无限格挡、无限能量。
4. `赛制改革 + 学校展出 + 交叉火力`：无限 AoE 伤害和无限能量。
5. `数学建模 + 召回通知+ + 被降至 0 费的例行训练`：无限伤害。
6. `数学建模 + 召回通知+ + 被降至 0 费的战术转移`：无限格挡。
7. `两张召回通知+`：无限出牌次数，但 Mod 自身没有直接伤害或格挡收益。

当前已实施两项调整：

- 时间管理大师改为每台机器人每回合最多触发一次，2～3 台机甲只能提供 2～3 次收益，不能 A/B 交替无限退款。
- 召回通知升级改为归队 2，费用保持 1，不再出现两张升级召回通知无条件互抽。

因此上面的时间管理大师简单无限和两张召回通知纯出牌无限已经关闭。数学建模将特定循环件临时降至 0 费的高门槛无限仍保留。

## 根本原因

### 赛制改革让借用不再消耗借用资源

`BorrowUtils.TryBorrow` 的顺序是：

1. 给一台未借用机甲施加 `BorrowedPower(1)`。
2. 调用 `RuleOverhaulPower.OnBorrow`。
3. 赛制改革抽 1 张牌、获得格挡、调用 `ReduceOneBorrow`。
4. `ReduceOneBorrow` 立即移除刚施加的 1 层借用。
5. 原机甲重新成为可借用目标。

因此，只要借用层数为 1，同一台机甲可以被每张借用牌反复借用。

代码：

- `LaughmanCode/Mechs/BorrowUtils.cs:38-64`
- `LaughmanCode/Mechs/RuleOverhaulPower.cs:20-25`

### 出牌结束后才进入弃牌堆

卡牌执行 `OnPlay` 和 `AfterCardPlayed` 时仍在 `Play` 堆，全部效果结束后才进入弃牌堆。因此两张互相抽取的牌可以稳定交替循环。

原版流程：`CardModel.OnPlayWrapper`，反编译文件约 `285868-285960`。

## 修复前的严格无限

### 1. 赛制改革 + 两张学校展出

组件：

- `赛制改革`已打出
- 两张`学校展出`
- 至少一台存活、未借用机甲
- 抽牌堆和弃牌堆已经压缩到能让两张学校展出互抽

单次学校展出：

- 费用：0
- 借用 1
- 赛制改革：抽 1、获得 1/3 格挡、立即归队 1
- 学校展出：借用成功后获得 2/3 能量
- 机甲状态：借用 1 后立即归队，循环资源不减少

两张学校展出交替时，每次都能抽回另一张。

每循环净变化：

| 资源 | 变化 |
|---|---:|
| 能量 | `+4`，升级后最多 `+6` |
| 格挡 | `+2`，赛制改革升级后 `+6` |
| 借用层数 | 0 |
| 手牌数量 | 0 |
| 伤害 | 0 |

判定：**严格无限能量、格挡和出牌次数**。

### 2. 赛制改革 + 学校展出 + 召回通知+

组件：

- `赛制改革`已打出
- 一张`学校展出`
- 一张升级后的`召回通知`（0 费）
- 至少一台机甲
- 小牌堆可使两张牌交替抽取

循环：

1. 学校展出借用机甲。
2. 赛制改革抽回召回通知、加格挡，并立即归队。
3. 学校展出获得 2/3 能量。
4. 打出召回通知+，抽回学校展出。

每循环净变化：

| 资源 | 变化 |
|---|---:|
| 能量 | `+2`，学校展出升级后 `+3` |
| 格挡 | `+1`，赛制改革升级后 `+3` |
| 借用层数 | 0 |
| 手牌数量 | 0 |
| 伤害 | 0 |

判定：**严格无限能量、格挡和出牌次数**。

### 3. 数学建模 + 召回通知+ + 例行训练

### 3. 赛制改革 + 学校展出 + 现场答辩

组件：

- `赛制改革`已打出
- 一张`学校展出`
- 一张`现场答辩`
- 至少一台机甲
- 小牌堆能让两张牌互抽

循环：

1. 学校展出借用，赛制改革抽到现场答辩、加格挡并立即归队；学校展出获得 2 能量。
2. 现场答辩花费 1 能量，先造成伤害和获得格挡，再次借用；赛制改革抽回学校展出、加格挡并立即归队，现场答辩再重复一次攻防。

每循环净变化：

| 资源 | 变化 |
|---|---:|
| 能量 | `+1`，学校展出升级后 `+2` |
| 伤害 | `+12`，现场答辩升级后 `+16` |
| 现场答辩格挡 | `+6`，升级后 `+10` |
| 赛制改革格挡 | `+2`，升级后 `+6` |
| 借用层数 | 0 |

判定：**严格无限伤害、格挡和能量**。这是当前最直接、最稳定的无限胜利组合。

### 4. 赛制改革 + 学校展出 + 交叉火力

结构与上一组合相同：

1. 学校展出获得 2 能量并通过赛制改革抽到交叉火力。
2. 交叉火力花费 1，借用成功后伤害翻倍；赛制改革抽回学校展出并立即归队。

每循环净赚 1 能量，并对所有敌人造成 `14/18` 点基础伤害。若击杀敌人，交叉火力自身还会继续追加波次。

判定：**严格无限 AoE 伤害和能量**。

### 5. 数学建模 + 召回通知+ + 例行训练

组件：

- `数学建模`抽到`例行训练`，使它本回合费用从 1 降到 0
- 一张升级后的`召回通知`
- 至少一台存活机甲，使例行训练抽 2
- 牌堆压缩到两张循环牌

循环：

1. 0 费例行训练造成伤害并抽回召回通知+。
2. 0 费召回通知+抽回例行训练。

每循环净变化：

| 资源 | 变化 |
|---|---:|
| 能量 | 0 |
| 伤害 | `8/11` |
| 手牌数量 | 0 |

判定：**严格无限伤害**。数学建模本身会消耗，但降费持续整回合，启动后不再需要数学建模。

如果同时存在学校展出或熬夜调车，还可以在循环中无限增加能量。

### 6. 数学建模 + 召回通知+ + 战术转移

组件同上，但数学建模将`战术转移`降至 0 费。

循环：

1. 战术转移给玩家和机甲格挡并抽回召回通知+。
2. 召回通知+抽回战术转移。

判定：**严格无限格挡**，没有直接伤害。

### 7. 两张召回通知+

两张升级后的召回通知都是 0 费、抽 1、不会消耗。

在两张牌可互抽的小牌堆中，可以无限交替打出。

Mod 自身直接收益：

- 无能量
- 无伤害
- 无格挡
- 无借用时归队没有收益

但它会无限增加“打出牌”的次数。当前 Mod 的`超级电容`每 17 张只给机甲飞坡，不直接击杀敌人；与原版或其他 Mod 中“每打若干张牌造成伤害/获得格挡”的效果组合时，可能成为外部无限胜利条件。

判定：**严格出牌无限，单独无胜利收益**。

## 伪无限与高循环

### 熬夜调车循环

`熬夜调车`为 0 费，每次 `TuneOnce`：

- 获得 1 能量
- 失去 1 生命
- 造成伤害
- 强化一台机甲

借用成功会重复一次。如果有赛制改革，借用会立即归队并抽 1，使熬夜调车容易进入循环。

限制：每次完整借用版本失去 2 生命，因此没有免疫自损或治疗闭环时会被玩家生命限制。

判定：**伪无限，通常在死亡前已能击杀敌人**。

### 清退通知

0 费，获得 2/3 能量并可消耗手牌，但自身消耗，且每次降低一层队员。

限制：

- 自身只能使用一次
- 队员层数有限
- 不抽牌

判定：**爆发能量，不是无限**。

### 集训封闭与数学建模

两者都能产生大量抽牌和能量修正，但自身消耗：

- 集训封闭借用成功时费用净 0，抽 3/4。
- 数学建模借用成功时实际净花费 1，抽 3/4并使其费用 -1。

它们是无限启动器，不是循环本体。

### 世界技能大赛

生成免费罕见/稀有牌，但显式排除自身 ID；生成牌池有限且依赖随机，无法保证递归生成世界技能大赛。

判定：**高随机爆发，不是严格无限**。

### 感谢开源 / 冲击UL / 剑指春茧

会生成或自动打出有限数量部署牌：

- 感谢开源：生成 1 张免费部署牌
- 冲击UL：生成 3 张免费部署牌
- 剑指春茧：自动打出 3/5 张牌组内召唤牌的战斗副本

生成数量固定，相关卡通常消耗，不能无限递归。

判定：**有限爆发**。

### 量产流水线

每场第二次及以后召唤机甲获得 1 能量，同名叠加也算召唤。

但当前可生成的免费召唤牌数量有限，召唤牌多数消耗；没有稳定无限生成召唤牌的闭环。

判定：**高能量爆发，不是独立无限**。

## 风险排序

| 风险 | 组合 | 原因 |
|---|---|---|
| 极高 | 赛制改革 + 学校展出 + 现场答辩 | 三张角色牌构成无限伤害、格挡和能量 |
| 极高 | 赛制改革 + 学校展出 + 交叉火力 | 无限 AoE 伤害，组件均可重复使用 |
| 极高 | 赛制改革 + 两张学校展出 | 组件少、无需降费、无限能量和格挡 |
| 极高 | 数学建模 + 召回通知+ + 例行训练 | 无限伤害，组件均在角色卡池 |
| 高 | 赛制改革 + 学校展出 + 召回通知+ | 无限能量和格挡，组件稳定 |
| 中 | 两张召回通知+ | 单独不赢，但容易接外部按出牌触发 |
| 中 | 赛制改革 + 熬夜调车循环 | 生命限制，但输出和能量很高 |

## 建议修复方向

目标是保留“需要主动压缩牌组、降费和多件组件”的高门槛无限，但拆掉三张角色牌即可稳定启动的简单无限。

已实施方案：

1. `时间管理大师`改为每台机器人每回合第一次借用时触发抽牌、格挡和归队。两台机甲每回合最多触发两次，三台最多三次，收益随铺场增长但必然终止。
2. `召回通知`升级不再降为 0 费，改为归队量 `1→2`。它仍是优质循环件，但不能用两张副本无条件纯出牌无限。
3. 保留`数学建模`的本回合降费。玩家仍可通过数学建模、两张循环牌、足够小的牌组和正确抽牌顺序搭出高门槛无限，符合允许无限的目标。
4. 暂时不改`学校展出`。失去赛制改革的即时退款后，每台机甲通常只能提供一次借用，学校展出变成有限爆发能量，而不是无限引擎。

这套方案会消除：

- 赛制改革 + 两张学校展出，以及两三台机甲交替借用的变体
- 赛制改革 + 学校展出 + 现场答辩/交叉火力
- 赛制改革 + 学校展出 + 召回通知+
- 两张召回通知+的无条件纯出牌无限

但保留：

- 数学建模把特定循环件降到 0 费后形成的构筑型无限
- 需要多张稀有/升级牌、压缩牌组与明确启动顺序的伪无限
- 熬夜调车以生命作为代价的高爆发循环

备选的更强硬方案：

1. `赛制改革`改成每回合第一次借用触发。
2. `学校展出`增加消耗，或改为每回合第一次借用时获得能量。
3. `召回通知+`不要降到 0 费；可改为增加抽牌或归队量。
4. 对`数学建模`增加“费用最低降至 1”限制，或只降低非攻击牌/非抽牌牌。

其中只修复第 1 条仍不能消除两张召回通知+的纯出牌无限；只修复召回通知+也不能消除两张学校展出配赛制改革的资源无限。
